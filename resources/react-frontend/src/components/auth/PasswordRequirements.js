import React from 'react';

export function PasswordRequirements() {
    return (
        <p>
            Note: Password must be at least 8 characters, and include one of each of the following:<br/>
            uppercase letter, lowercase letter, special character, number
        </p>
    );
}
